<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="fixed.list">
      <field name="name">fixed list</field>
      <field name="model">fixed.fixed</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="fixed.action_window">
      <field name="name">fixed window</field>
      <field name="res_model">fixed.fixed</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="fixed.action_server">
      <field name="name">fixed server</field>
      <field name="model_id" ref="model_fixed_fixed"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="fixed" id="fixed.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="fixed.menu_1" parent="fixed.menu_root"/>
    <menuitem name="Menu 2" id="fixed.menu_2" parent="fixed.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="fixed.menu_1_list" parent="fixed.menu_1"
              action="fixed.action_window"/>
    <menuitem name="Server to list" id="fixed" parent="fixed.menu_2"
              action="fixed.action_server"/>
    -->
  </data>
</odoo>